# GRDL Optional Dependencies
# Install based on which modules you need

# SAR Format Readers (grdl.IO.sar)
# ---------------------------------
# sarkit is preferred; sarpy is the fallback
sarpy>=1.3.0
sarkit>=1.3.0

# Raster / GeoTIFF format support (grdl.IO.geotiff, grdl.IO.nitf, grdl.IO.sar.biomass)
rasterio>=1.2.0

# HDF5 format support (grdl.IO.hdf5, grdl.IO.multispectral.viirs, grdl.IO.ir)
h5py>=3.0.0

# JPEG2000 format support (grdl.IO.jpeg2000, grdl.IO.eo)
glymur>=0.9.0

# Multispectral / Hyperspectral (grdl.IO.multispectral)
xarray>=2022.0.0
spectral>=0.23

# Geolocation (grdl.geolocation.eo, grdl.geolocation.elevation)
# ---------------------------------------------------------------
pyproj>=3.4.0

# Co-Registration (grdl.coregistration.feature_match)
# ----------------------------------------------------
opencv-python-headless>=4.5.0

# Catalog / Remote Downloads (grdl.IO.sar.biomass_catalog)
# ---------------------------------------------------------
requests>=2.25.0

# Visualization — examples only (grdl.example)
# ----------------------------------------------
matplotlib>=3.5.0

# Installation examples:
# pip install -r requirements.txt -r requirements-optional.txt  # Install all
# pip install -r requirements.txt sarpy sarkit                  # Just SAR readers
# pip install -r requirements.txt rasterio                      # Just GeoTIFF/NITF
# pip install -r requirements.txt h5py                          # Just HDF5
