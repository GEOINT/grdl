# GRDL Optional Dependencies
# Install based on which data formats you need to read

# SAR Format Readers
# -----------------

# SICD/CPHD format support (requires SARPY)
sarpy>=1.3.0

# GeoTIFF/Raster format support (BIOMASS, GRD, geocoded imagery)
rasterio>=1.2.0

# JPEG2000 format support (Sentinel-2, Pleiades, SPOT)
# rasterio with GDAL JP2 driver OR glymur (fallback for better 15-bit support)
glymur>=0.9.0

# Co-Registration
# ----------------

# Feature-based co-registration (SIFT/ORB + RANSAC)
opencv-python-headless>=4.5.0

# Installation examples:
# pip install -r requirements.txt -r requirements-optional.txt  # Install all
# pip install -r requirements.txt rasterio  # Just rasterio for BIOMASS/GRD
# pip install -r requirements.txt sarpy     # Just SARPY for SICD/CPHD