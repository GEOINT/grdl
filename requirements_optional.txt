# GRDL Optional Dependencies
# Install based on which features you need

# SAR Format Readers
# ------------------

# SICD/CPHD format support (grdl.IO.sar)
sarpy>=1.3.0

# GeoTIFF / Raster format support (grdl.IO.biomass, grdl.IO.sar)
rasterio>=1.2.0

# Co-Registration
# ----------------

# Feature-based co-registration via SIFT/ORB + RANSAC (grdl.coregistration.feature_match)
opencv-python-headless>=4.5.0

# Catalog
# -------

# Remote catalog queries and product download (grdl.IO.catalog)
requests>=2.25.0

# Installation examples:
# pip install -r requirements.txt -r requirements_optional.txt  # Install all
# pip install -r requirements.txt sarpy      # Just SARPY for SICD/CPHD
# pip install -r requirements.txt rasterio   # Just rasterio for BIOMASS/GRD
