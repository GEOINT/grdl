# GRDL Core Dependencies
# Required for basic functionality

# Core numerical computing
# Used by: virtually every module (IO, geolocation, image_processing, coregistration, data_prep)
numpy>=2.0.0

# Scientific computing (interpolation, ndimage filters)
# Used by: coregistration/utils.py, geolocation/sar/gcp.py, image_processing/ortho/ortho.py
scipy>=1.11.0
