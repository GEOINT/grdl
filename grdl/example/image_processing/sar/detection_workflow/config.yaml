input:
  sicd_path: /Users/duanesmalley/SAR_DATA/SICD/2025-06-20-02-42-41_UMBRA-05_SICD.nitf
  chip_size: 10000

sublook:
  num_looks: 7
  dimension: azimuth

detection:
  smooth_win: 3
  dom_window: 3
  sigma: 3.0
  morph_size: 3

benchmark:
  source: file            # "file" = use the loaded chip, "synthetic" = generate via BenchmarkSource
  synthetic_size: small  # size preset when source is "synthetic" (small / medium / large)
  iterations: 3
  warmup: 1

display:
  vmax_percentile: 98
