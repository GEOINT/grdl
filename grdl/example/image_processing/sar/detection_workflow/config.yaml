input:
  sicd_path: /code/data/umbra_scenes/2026-01-13-14-11-30_UMBRA-07/2026-01-13-14-11-30_UMBRA-07_SICD.nitf
  chip_size: 10000

sublook:
  num_looks: 7
  dimension: azimuth

detection:
  smooth_win: 7
  dom_window: 3
  sigma: 3
  morph_size: 3

benchmark:
  source: file            # "file" = use the loaded chip, "synthetic" = generate via BenchmarkSource
  synthetic_size: small  # size preset when source is "synthetic" (small / medium / large)
  iterations: 3
  warmup: 1

display:
  vmax_percentile: 98
